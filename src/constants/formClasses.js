export const inputClass =
  "w-full py-2 pl-7 pr-3 mb-2.5 border-2 border-fantasy-blue/0 focus:border-fantasy-blue-active focus:border-2 rounded-lg outline-none";

// export const inputContainer = "flex";

export const iconClass = (icon) => {
  return `${icon} absolute color-earth-brown my-auto ml-2`;
};
